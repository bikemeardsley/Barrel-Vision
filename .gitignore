# Tooling / local agent config
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~
.idea/
.vscode/

# Build / packaging output (buildless project: nothing to compile, but keep store zips out)
*.zip
dist/
build/

# Dependencies (none expected — vanilla MV3 — but guard against accidental installs)
node_modules/
package-lock.json

# Local-only: kept out of the public repo (origin userscript, dev-only test harness, internal docs).
# docs/images/ stays tracked — the README displays those screenshots — EXCEPT the store-submission
# assets below, which are local-only.
docs/images/edge-store/
userscript/
tests/
package.json
docs/PROJECT_BARREL_VISION.md
docs/PUBLISHING.md
docs/fantasy_baseball_standards.md

# Local scratch / logs
*.log
